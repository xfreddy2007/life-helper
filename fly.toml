app = "life-helper-bot"
primary_region = "sin"

[build]

[env]
  PORT = "3000"
  NODE_ENV = "production"

# Run migrations then seed default categories before each new version goes live.
# Fly executes this in a temporary machine with full image access before
# routing traffic to the updated app machines.
# The seed uses upsert so it is safe to re-run; it only inserts categories
# that do not already exist.
[deploy]
  release_command = "node node_modules/.bin/prisma migrate deploy --schema packages/database/prisma/schema.prisma && node_modules/.bin/tsx packages/database/prisma/seed/seed.ts"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = "connections"
    hard_limit = 25
    soft_limit = 20

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
